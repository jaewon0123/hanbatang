<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.hanbatang.mapper.BoardMapper">
	<select id="getAllPost" resultType="com.hanbatang.dto.Board">
		SELECT * FROM board
	</select>
	
	<insert id="insertBoard" parameterType="com.hanbatang.dto.Board">
		INSERT INTO board (board_no, member_no, member_name, member_nickname, board_title, board_text, board_public)
		VALUES (board_seq.NEXTVAL, member_seq.NEXTVAL, member_name, member_nickname, #{board_title}, #{board_text}, #{board_public})
	</insert>
</mapper>        